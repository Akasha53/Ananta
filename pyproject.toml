[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [
  "text-generation-webui",
  "text-generation-webui_BACKUP",
  "alembic/versions",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
