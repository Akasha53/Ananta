# Ananta - dépendances sans la pile ML (torch / sentence-transformers).
#
# Suffit pour : API, recherche d'entité, workers Celery, exports, interface.
# Le classifieur d'intention par embeddings est alors désactivé — Ananta
# retombe sur ses heuristiques, qui couvrent les cas courants.
#
# Pour l'installation complète : pip install -r requirements.txt

# === Core Framework ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
pydantic>=2.0.0

# === Database ===
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9

# === Async Tasks ===
celery>=5.3.0
redis>=5.0.0
kombu>=5.3.0

# === HTTP & Networking ===
requests>=2.31.0
httpx>=0.25.0
aiohttp>=3.9.0
mcp[cli]>=1.26.0,<2

# === OSINT Tools ===
python-whois>=0.8.0
duckduckgo-search>=4.0.0
googlesearch-python>=1.2.0

# === Entity Research ===
phonenumbers>=8.13.0

# === Exports ===
reportlab>=4.0.0
openpyxl>=3.1.0

# === System Monitoring ===
psutil>=5.9.0

# === Security & Utils ===
python-dotenv>=1.0.0
python-multipart>=0.0.6
beautifulsoup4>=4.12.0
lxml>=4.9.0
